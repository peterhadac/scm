---
// ponytail: icon + wordmark lockup per Design.md — no logo image/logos config,
// so this replaces Starlight's default SiteTitle instead of layering on top of it.
// Rendering itself lives in Wordmark.astro (issue #121), shared with the new
// non-Starlight Layout.astro; this component only adapts Starlight's props.
import Wordmark from './Wordmark.astro';
const { siteTitleHref } = Astro.locals.starlightRoute;
---

<Wordmark href={siteTitleHref} />
